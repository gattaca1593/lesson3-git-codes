 ## git codes
 
Вот список 30 команд Git с кратким объяснением:

git init: Инициализирует новый Git репозиторий.

git clone [url]: Клонирует (скачивает) репозиторий из удаленного источника.

git add [file]: Добавляет файл в индекс для коммита.

git add . or git add -A: Добавляет все изменения в индекс.

git commit -m "[сообщение]": Записывает изменения в репозиторий.

git status: Показывает состояние рабочего каталога.

git pull: Скачивает изменения из удаленного репозитория и объединяет их с текущей веткой.

git push: Отправляет коммиты в удаленный репозиторий.

git branch: Показывает список всех локальных веток.

git branch [branch-name]: Создает новую ветку.

git checkout [branch-name]: Переключается на указанную ветку.

git merge [branch-name]: Сливает указанную ветку в текущую ветку.

git log: Показывает историю коммитов.

git diff: Показывает разницу между рабочим каталогом и индексом.

git reset: Сбрасывает текущий HEAD в указанное состояние.

git rebase: Перемещает или объединяет коммиты на новую базу.

git stash: Временно сохраняет изменения, которые вы не хотите коммитить сразу.

git tag: Отмечает специфический коммит "меткой".

git fetch: Загружает все объекты из удаленного репозитория.

git remote: Показывает все удаленные репозитории.

git rm: Удаляет файлы из индекса и рабочей директории.

git mv: Перемещает или переименовывает файл, каталог, или символическую ссылку.

git bisect: Используется для поиска коммита, который вызвал ошибку.

git cherry-pick: Применяет изменения в указанном коммите к текущей ветке.

git clean: Удаляет неотслеживаемые файлы из рабочей директории.

git revert: Создает новый коммит, который отменяет все изменения, внесенные предыдущим коммитом.

git show: Показывает информацию о любом объекте git.

git blame: Показывает информацию о том, кто и когда внес изменения в каждую строку файла.

git config: Позволяет получить и установить настройки Git.

git help: Показывает справочную информацию о Git.